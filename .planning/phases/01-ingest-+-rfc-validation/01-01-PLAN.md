---
phase: 01-ingest-+-rfc-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/App.css
  - src/index.css
  - src/vite-env.d.ts
  - public/vite.svg
  - src/assets/react.svg
autonomous: true
must_haves:
  truths:
    - "User can submit a M3U8 URL, channel/VOD ID, or local M3U8 file and trigger analysis."
    - "User sees validation issues with severity and fix hints when playlist rules are violated."
    - "User sees playlist classification for master/variant and live/VOD."
    - "User sees an ABR ladder table with bitrate, resolution, and codecs extracted from the playlist."
  artifacts:
    - path: "src/analysis/playlist/parseM3U8.ts"
      provides: "M3U8 parsing wrapper"
    - path: "src/analysis/playlist/validatePlaylist.ts"
      provides: "RFC 8216 validation issues"
    - path: "src/analysis/playlist/classifyPlaylist.ts"
      provides: "Master/variant + live/VOD classification"
    - path: "src/analysis/ladder/extractLadder.ts"
      provides: "ABR ladder extraction"
    - path: "src/components/InputPanel.tsx"
      provides: "Input type selector + analyze action"
    - path: "src/components/ValidationPanel.tsx"
      provides: "Validation feedback UI"
    - path: "src/components/ClassificationBadges.tsx"
      provides: "Playlist classification display"
    - path: "src/components/LadderTable.tsx"
      provides: "Ladder table UI"
  key_links:
    - from: "src/components/InputPanel.tsx"
      to: "src/hooks/usePlaylistAnalysis.ts"
      via: "onAnalyze callback"
      pattern: "onAnalyze"
    - from: "src/hooks/usePlaylistAnalysis.ts"
      to: "src/analysis/playlist/parseM3U8.ts"
      via: "parseM3U8"
      pattern: "parseM3U8"
    - from: "src/components/ValidationPanel.tsx"
      to: "AnalysisResult.validation.issues"
      via: "map render"
      pattern: "issues\.map"
    - from: "src/components/LadderTable.tsx"
      to: "AnalysisResult.ladder"
      via: "map render"
      pattern: "ladder\.(video|audio)"
---

<objective>
Establish the Phase 1 project scaffold and a base UI shell for ingest and results.

Purpose: Create a stable React + Vite foundation for the ingest/validation features.
Output: A running app shell with placeholders for input and analysis results.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ingest-+-rfc-validation/01-CONTEXT.md
@.planning/phases/01-ingest-+-rfc-validation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite React + TypeScript scaffold</name>
  <files>package.json, package-lock.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/App.css, src/index.css, src/vite-env.d.ts, public/vite.svg, src/assets/react.svg</files>
  <action>
    Scaffold a Vite React TypeScript app in the repo root (use the react-ts template) and install dependencies.
    Keep default scripts (dev/build/preview) intact and ensure the app builds without additional configuration.
  </action>
  <verify>npm run build</verify>
  <done>Vite app builds successfully and renders the default App component.</done>
</task>

<task type="auto">
  <name>Task 2: Create the base Phase 1 UI shell</name>
  <files>src/App.tsx, src/App.css, src/index.css</files>
  <action>
    Replace the template UI with a shell layout that includes:
    - Header/title for "Stream ABR Advisor" and a short Phase 1 subtitle.
    - Two main sections: an "Ingest" panel area and a "Results" panel area (placeholders for now).
    - Clean spacing and a neutral, light background to support the future validation and ladder panels.
    Keep markup minimal and semantic; do not add analysis logic yet.
  </action>
  <verify>npm run build</verify>
  <done>App shell shows header plus labeled Ingest and Results sections with placeholder content.</done>
</task>

</tasks>

<verification>
`npm run build` completes without errors.
</verification>

<success_criteria>
Project scaffold exists and renders a stable UI shell ready for ingest and results components.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingest-+-rfc-validation/01-01-SUMMARY.md`
</output>
