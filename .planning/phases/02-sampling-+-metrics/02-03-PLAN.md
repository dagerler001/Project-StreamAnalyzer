---
phase: 02-sampling-+-metrics
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/components/BitrateChart.tsx
  - src/components/CodecSummary.tsx
  - src/App.tsx
  - src/App.css
  - src/index.css
autonomous: true

must_haves:
  truths:
    - "User can see detected audio and video codecs for the sampled rendition."
    - "User can view bitrate-over-time results with raw and smoothed series."
    - "Partial or failed samples surface diagnostics with a retry option."
  artifacts:
    - path: "src/components/BitrateChart.tsx"
      provides: "Bitrate-over-time visualization with rolling average"
    - path: "src/components/CodecSummary.tsx"
      provides: "Audio/video codec display for the sample"
    - path: "src/App.css"
      provides: "Styling for sampling metrics UI"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/BitrateChart.tsx"
      via: "sampleState.result.bitrateSeries"
      pattern: "BitrateChart"
    - from: "src/App.tsx"
      to: "src/components/CodecSummary.tsx"
      via: "sampleState.result.codecs"
      pattern: "CodecSummary"
---

<objective>
Render codec details and bitrate-over-time charts for the sampled window, including partial-result diagnostics.

Purpose: Deliver the Phase 2 metrics experience in the Results panel.
Output: Chart and codec components styled for the existing UI.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/App.tsx
@src/App.css
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build codec and bitrate chart components</name>
  <files>
src/components/BitrateChart.tsx
src/components/CodecSummary.tsx
  </files>
  <action>
Create two new components for sampled metrics:
- CodecSummary: show video codecs and audio codecs separately, listing friendly labels and raw codec strings when available. Display a placeholder when codecs are missing.
- BitrateChart: render an SVG area chart using bitrate points with both raw bitrate and rollingAverage lines. Scale using min/max bitrate; include labels for start/end times and a legend for raw vs smoothed.

Keep the implementation dependency-free and resilient to empty datasets.
  </action>
  <verify>npm run build</verify>
  <done>Components render with sample data and handle empty/partial states gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate metrics display and styling</name>
  <files>
src/App.tsx
src/App.css
src/index.css
  </files>
  <action>
Wire the new metrics UI into the Results panel:
- Render CodecSummary and BitrateChart when sampleState is success.
- Show loading and error states for sampleState, including a "Retry sample" button wired to retrySample.
- Display a warning banner when sampleState.result.reliable is false, listing diagnostics such as missing headers or CORS failures.
- Add CSS classes for sampling controls, codec list, chart container, and warning banner; follow existing color variables and layout patterns.

Keep layout responsive and aligned with the existing panel styling.
  </action>
  <verify>npm run build</verify>
  <done>Sample metrics render in the Results panel with diagnostics, styling, and retry support.</done>
</task>

</tasks>

<verification>
npm run build
</verification>

<success_criteria>
- Codecs and bitrate charts appear after a successful sample run.
- Partial results are clearly flagged and include a retry action.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sampling-+-metrics/02-03-SUMMARY.md`
</output>
